//! p2p-sync-mcp MCP server binary (stdio transport)。
//!
//! Phase 1 placeholder。Phase 4 で 10 #[tool] (sync.ping + 9 daemon RPC bridge) を組む。

fn main() -> anyhow::Result<()> {
    eprintln!("p2p-sync-mcp v{}: not implemented yet (Phase 4)", env!("CARGO_PKG_VERSION"));
    Ok(())
}
