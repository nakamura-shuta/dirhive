//! p2p-sync daemon binary。
//!
//! Phase 1 placeholder。Phase 3 で CLI / 多重起動 check / SyncRuntime / listener /
//! watcher / receive_loop を組む。

fn main() -> anyhow::Result<()> {
    eprintln!("p2p-sync v{}: not implemented yet (Phase 3)", env!("CARGO_PKG_VERSION"));
    Ok(())
}
